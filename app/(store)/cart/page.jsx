import CartClient from "@/components/cart-client"

export const metadata = {
  title: "Cart | Next-Cart",
  description: "Review your shopping cart",
}

export default function CartPage() {
  return <CartClient />
}
